# API Configuration
# Backend server URL for API calls and WebSocket connections
VITE_API_URL=http://localhost:3000

# Environment (automatically set by Vite - usually don't need to override)
# NODE_ENV=development

# Development Notes:
# - VITE_API_URL should point to your backend server
# - In production, this will be automatically set by the deployment platform
# - For local development, ensure the backend server is running on the specified port