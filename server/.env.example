# Server Configuration
PORT=3000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# JWT Configuration (REQUIRED in production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Database Configuration
DATABASE_URL=postgresql://dueled_user:dueled_password@localhost:5433/dueled

# Redis Configuration
REDIS_URL=redis://localhost:6380

# CORS Configuration (optional - comma-separated additional origins)
# ADDITIONAL_CORS_ORIGINS=http://localhost:3000,http://localhost:5000

# WebSocket Configuration (optional - defaults provided)
WEBSOCKET_HEARTBEAT_INTERVAL=30000
WEBSOCKET_CONNECTION_TIMEOUT=60000
MAX_PLAYERS_PER_MATCH=2

# Rate Limiting Configuration (optional - defaults provided)
# Global rate limiting (15 minutes, 100 requests)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# API rate limiting (15 minutes, 60 requests)
API_RATE_LIMIT_WINDOW_MS=900000
API_RATE_LIMIT_MAX_REQUESTS=60

# Health check rate limiting (1 minute, 10 requests)
HEALTH_RATE_LIMIT_WINDOW_MS=60000
HEALTH_RATE_LIMIT_MAX_REQUESTS=10

# Server Configuration (optional - defaults provided)
REQUEST_TIMEOUT_MS=30000
HEALTH_CHECK_INTERVAL_MS=30000

# Connection Policies (optional - defaults provided)
CONNECTION_HEARTBEAT_INTERVAL=30000
CONNECTION_TIMEOUT=60000
CONNECTION_GRACE_PERIOD=2000

# Game Configuration Multipliers (optional - defaults provided)
# COUNTDOWN_TIMEOUT_MULTIPLIER=1.5
# COUNTDOWN_GRACE_MULTIPLIER=2.0
# ACTIVE_GRACE_MULTIPLIER=1.5